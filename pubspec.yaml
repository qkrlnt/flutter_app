name: pmu
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_svg: 2.2.3

  json_annotation: ^4.9.0
  dio: ^5.9.0
  pretty_dio_logger: ^1.4.0
  http: ^1.2.2

  equatable: ^2.0.7
  flutter_bloc: ^9.1.1
  copy_with_extension_gen: ^11.0.0
  rxdart: ^0.28.0

  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.10.4
  json_serializable: ^6.11.3

  flutter_lints: ^5.0.0

flutter_icons:
  android: "ic_launcher"
#  ios: true
  image_path: "assets/penguin-bird-animal-silhouette-cartoon-vector-icon-png_113897.jpg"
  min_sdk_android: 21

flutter:
  generate: true

  uses-material-design: true

  assets:
    - assets/svg/